defaultNamespace: kube-system

helm:
  releaseName: jspolicy

  chart: jspolicy

  repo: https://charts.loft.sh

  force: false

  timeoutSeconds: 0

  //values:
  //  replicas: 1

  valuesFrom:
  - configMapKeyRef:
      name: env-for-js
      namespace: kube-system 
      key: values.yaml