defaultNamespace: kube-system

kustomize:
  dir: ./kustomize

helm:
  releaseName: jspolicy

  chart: jspolicy

  repo: https://charts.loft.sh

  force: false

  timeoutSeconds: 0

  values:
    replicas: 1  