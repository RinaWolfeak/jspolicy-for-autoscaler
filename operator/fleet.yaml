defaultNamespace: kube-system

#kustomize:
#  dir: ./operator

helm:
  releaseName: jspolicy

  chart: jspolicy

  repo: https://charts.loft.sh

  force: false

  timeoutSeconds: 0

  #values:
  #  replicas: 1  
  valuesFrom:
  - configMapKeyRef:
      name: env-for-js
      namespace: kube-system